<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <div>
      <form class="form-inline" method="GET" action="/register" >
        <label for="username"><%= user !== undefined ? `Welcome back, ${user.email}!  ` : ''; %></label>
        <% if (user === undefined) { %>
        <button class="button btn-dark">Create account</button>
        </form>
        <small>Have an account? <a href="/login">Sign in</a></small>
        <% } else { %>
        <button class="button btn-dark" style="margin-left: 2px;">Log Out</button>
        <% } %>
      </form>
    </div>
  </nav>
</header>
